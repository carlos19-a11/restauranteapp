import 'package:flutter/material.dart';

import '../widgest/hedaders.dart';

class InicioPage extends StatelessWidget {
  const InicioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: HedersPage(),
    );
  }
}
